<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MenuActivity">

    <EditText
        android:id="@+id/input_sport_id"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:inputType="number"
        android:hint="Sport id"
        app:layout_constraintTop_toTopOf="parent"/>

    <EditText
        android:id="@+id/input_match_id"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:inputType="number"
        android:hint="Match id"
        app:layout_constraintTop_toBottomOf="@+id/input_sport_id"/>

    <Button
        android:id="@+id/button_search"
        android:layout_width="300dp"
        android:layout_height="70dp"
        android:text="@string/search"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/input_match_id" />

</androidx.constraintlayout.widget.ConstraintLayout>